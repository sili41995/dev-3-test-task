export interface LetterEl extends HTMLElement {
  _origIndex: number;
}

export interface Position {
  x: number;
  y: number;
}

export interface Rect {
  x: number;
  y: number;
  w: number;
  h: number;
}

export interface Offset {
  left: number;
  top: number;
}
